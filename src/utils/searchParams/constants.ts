import { MovieSearchFormSchema, TvSearchFormSchema } from './types';

export const DEFAULT_MOVIE_SEARCH_FORM = MovieSearchFormSchema.parse({});
export const DEFAULT_TV_SEARCH_FORM = TvSearchFormSchema.parse({});
