// all providers for a movie, for display
export const PROVIDER_JOIN = 'providers: media_provider(id: provider_id)';

// all credits and persons
export const CREDIT_PERSON_JOIN = 'credits: credit(*, person(*))';
